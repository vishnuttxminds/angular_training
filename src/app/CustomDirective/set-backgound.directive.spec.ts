import { SetBackgoundDirective } from './set-backgound.directive';

describe('SetBackgoundDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBackgoundDirective();
    expect(directive).toBeTruthy();
  });
});
