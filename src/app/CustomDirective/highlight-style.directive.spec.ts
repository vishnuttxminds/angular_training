import { HighlightStyleDirective } from './highlight-style.directive';

describe('HighlightStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightStyleDirective();
    expect(directive).toBeTruthy();
  });
});
